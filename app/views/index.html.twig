{% extends 'layout.html.twig' %}
{% block content %}
    This is the homepage

    <form action="" method="post">
        {{ form_widget(form) }}
    </form>

    <table class="table">
        <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Is done</th>
        </tr>
        </thead>
        <tbody>
        {% for task in tasks %}
            <tr>
                <th scope="row">{{ task.id }}</th>
                <td>{{ task.name }}</td>
                <td>{{ task.isDone }}</td>
            </tr>
        {% endfor %}

       </tbody>
    </table>
{% endblock %}

